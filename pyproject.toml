[project]
name = "mcp-tool"
version = "0.1.0"
description = "Toy MCP server exposing random animal and d20 tools using the official MCP Python SDK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.21.1",
]
